{
  "name": "media-eligibility-inspector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MV3 확장 스캐폴드: 미디어 탐지 + 다운로드 적격성 판정 (비DRM·비암호화, 우회 없음)",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "esbuild": "^0.23.0",
    "typescript": "^5.6.2"
  }
}
